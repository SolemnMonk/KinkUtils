package monk.solemn.kutils.shoot;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

import monk.solemn.kutils.objects.Shoot;

public class ShootDownloader {
	private static final Logger logger = LogManager.getLogger(ShootDownloader.class);
	
	public static void DownloadShootVideos(WebDriver driver, Shoot shoot) {
		
	}
	
	public static void DownloadShootImages(WebDriver driver, Shoot shoot) {
		
	}
}
