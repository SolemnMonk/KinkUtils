package monk.solemn.kutils.application;

import monk.solemn.kutils.objects.Task;
import monk.solemn.kutils.plugin.api.PluginBase;

public class TaskValidator {
	public static boolean isValidTask(PluginBase plugin, Task task) {
		// TODO Fill me in, ho.
		/* Check for the following:
		 *   The plugin and task aren't null
		 *   Make sure the plugin is an instance of the right interface for the task
		 *   Make sure the task's target is valid for the action 
		 */
		return true;
	}
}
